====
TODO
====

kay
----

* manage.py で dumpdata と loaddata を作る

* manage.py で国際化メッセージの処理をできるようにする

* db.kind() に app の prefix を付けるべきかも知れない...が判りづらいし
  トラブルの元になるかもしれないなあ

  実装してコミットしてみた。色々試す。テスト書くのも良いよね。

* debugger でペーストする先を変えないとまずいかも

* 下記のような事をしてくれるスクリプトを用意する

.. code-block:: console

   $ mkdir yourproject
   $ cd yourproject
   $ ln -s ../kay/kay .
   $ ln -s ../kay/manage.py .
   $ cp ../kay/settings.py.sample ./settings.py
   $ cp ../kay/app.yaml.sample ./app.yaml
   $ cp ../kay/urls.py .

kay.utils.forms
----------------

* Media (javascript など) を書けると良いかな...

 * AjaxModelField みたいなのを作る

* ModelField の実装

 * 自動で選択肢をキャッシュするようにする


kay.auth
---------------

* user 認証をどうするか。抽象化したユーザクラスが要りそう

 * デベロッパーは GoogleUser を拡張できるようにしよう

 * 独自の認証を使いたい場合もあるよね

  * 独自認証の場合 https 関係で、ログイン画面ではドメインが変わるケース
    もある。(独自ドメインでは https を使えないので)

    ドメインまたがってクッキーを使えないのでその場合はドメインが変わる
    時に内部でランダムストリングを発行してセッション情報をやり取りする?
